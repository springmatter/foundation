import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import SmButton from "../plugin/components/SmButton.vue";

<Meta title="Components/SmButton" component={SmButton} />

# SmButton

A standard button component with optinal icon.

<Props of={SmButton} />

## Standard Buttons

<Preview>
  <Story name="default" height="70px">
    {{
      components: { SmButton },
      template: `
      <div class="flex">
        <SmButton class="ma2">Primary</SmButton>
        <SmButton kind="secondary" class="ma2">Secondary</SmButton>
      </div>
      `
    }}
  </Story>
</Preview>

## Icon Buttons

<Preview>
  <Story name="icons" height="120px">
    {{
      components: { SmButton },
      template: `
      <div class="flex flex-wrap">
        <SmButton icon="plus" class="ma2">Primary</SmButton>
        <SmButton icon="sun" kind="secondary" class="ma2">Secondary</SmButton>
        <div class="w-100"></div>
        <SmButton icon="plus" class="ma2">Primary</SmButton>
        <SmButton icon="sun" kind="secondary" class="ma2">
          Secondary
        </SmButton>
      </div>
      `
    }}
  </Story>
</Preview>

## Icon only

Prop `kind` must be set to `icon` and prop `icon` must be specified.

<Preview>
  <Story name="icon" height="120px">
    {{
      components: { SmButton },
      template: `
      <div class="flex">
        <SmButton icon="plus" kind="icon" tooltip="Plus"></SmButton>
        <SmButton icon="feather" kind="icon" tooltip="Feather"></SmButton>
        <SmButton icon="sun" kind="icon" tooltip="Sun"></SmButton>
        <SmButton icon="archive" kind="icon" tooltip="Archive"></SmButton>
      </div>
      `
    }}
  </Story>
</Preview>

## `<button>` only Buttons

If you just need to make something clickable and focusable, a standard `<button>` tag is useful. `<button>` should be used instead of `<a>` or `<router-link>` when the clickable element doesn't toggle a route change or a page location change on a page of text. Using `<button>` to wrap clickable elements makes them more accessible by default.

<Preview>
  <Story name="button" height="120px">
    {{
      components: { SmButton },
      template: `
      <div class="flex">
        <button class="ma2">
          <small>A simple button</small>
        </button>
      </div>
      `
    }}
  </Story>
</Preview>
